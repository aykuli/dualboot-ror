import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles(() => ({
  addButton: {
    position: 'absolute',
    bottom: 12,
    right: 32,
  },
}));

export default useStyles;
